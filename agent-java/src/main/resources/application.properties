# The repository .env uses standard KEY=value syntax, which Spring Boot can
# import as a properties source.  This keeps the Java service aligned with the
# Python service without checking credentials into source control.
spring.config.import=optional:file:../.env[.properties]
